apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: starrupture-data
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      # 30 Gi base install + saves + Wine prefix + backup headroom.
      # Bump if you keep many backup tarballs on-volume.
      storage: 50Gi
